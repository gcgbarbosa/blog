---
const { title, subtitle = null } = Astro.props;
---

<div>
  <h1 class="text-3xl font-bold sm:text-3xl lg:text-4xl text-primary-content">{title}</h1>
  {
    subtitle && (
    <span class="text-2xl font-bold sm:text-xl lg:text-lg text-secondary-content/60">{subtitle}</span>
    )
  }
</div>
