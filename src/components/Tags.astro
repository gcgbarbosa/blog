---
const { tags } = Astro.props;
---

<div class="flex flex-row flex-wrap gap-y-2 gap-x-1">
    {
      tags.map(tag => (
        <div class="badge badge-dash badge-info">
          <a class="link link-hover link-primary" href={`/tags/${tag}`}>#{tag}</a>
        </div>
      ))
    }
  </div>

